<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="factura_electronica_res_partner" model="ir.ui.view">
        <field name="name">factura_electronica_res_partner</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='sales_purchases']" position="after">
                <page string="Factura Electrónica">
                    <group>
                        <group string="AFIP" priority="1">
                            <field name="nro_iibb"/>
                            <field name="fecha_inicio_actividades"/>
                        </group>
                        <group string="Estado" priority="2">
                            <field name="es_proveedor_estado"/>
                            <field name="proveedor_estado" attrs="{'invisible': [('es_proveedor_estado', '=', False)]}"/>
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
